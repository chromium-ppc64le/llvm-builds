# LLVM Builds for Compiling Chromium on PPC64LE

The Chromium source specifies the version of `clang`/`lld` that Chromium should
be compiled with.

This repo contains pre-built binaries for the specific releases.

